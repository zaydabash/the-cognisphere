# The Cognisphere — frontend environment configuration
# Copy to .env.local and adjust: cp frontend/.env.example frontend/.env.local

# Backend API URL used by the Vite dev-server proxy (requests to /api/* are
# proxied here with the /api prefix stripped). Used in `npm run dev`.
VITE_API_URL=http://localhost:8000
